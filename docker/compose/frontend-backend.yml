services:
  backend:
    build:
      context: ../..
      dockerfile: docker/Dockerfile.backend
    container_name: denttime_backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ../..
      dockerfile: docker/Dockerfile.frontend
    container_name: denttime_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:8000